
function showBoards() {
  // Display score
  fill(255, 255, 255); // White text
  textSize(24);
  textAlign(LEFT, TOP);
  text("Score: " + score, 10, 10);
}

function showHealth() {
  // Display score
  fill(255, 255, 255); // White text
  textSize(24);
  textAlign(LEFT, TOP);
  text("Health: " + health, 10, 30);
}